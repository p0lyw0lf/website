---
import type { BlogData } from "../../data/blog";

interface Props {
  data: BlogData;
}

const { data } = Astro.props;
---

{data.published.toLocaleDateString("en-US", { timeZone: "America/New_York" })}: <a
  href={data.url}>{data.title}</a
>
